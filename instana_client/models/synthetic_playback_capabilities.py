# coding: utf-8

"""
    Instana REST API documentation

    Documentation for INSTANA REST API

    The version of the OpenAPI document: 1.323.989
    Contact: support@instana.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class SyntheticPlaybackCapabilities(BaseModel):
    """
    SyntheticPlaybackCapabilities
    """ # noqa: E501
    browser_type: List[StrictStr] = Field(alias="browserType")
    execution_type: Optional[List[StrictStr]] = Field(default=None, alias="executionType")
    extensions: Optional[List[StrictStr]] = None
    synthetic_type: List[StrictStr] = Field(alias="syntheticType")
    __properties: ClassVar[List[str]] = ["browserType", "executionType", "extensions", "syntheticType"]

    @field_validator('browser_type')
    def browser_type_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['chrome', 'firefox']):
                raise ValueError("each list item must be one of ('chrome', 'firefox')")
        return value

    @field_validator('execution_type')
    def execution_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['CI/CD']):
                raise ValueError("each list item must be one of ('CI/CD')")
        return value

    @field_validator('extensions')
    def extensions_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['GitHub', 'PW']):
                raise ValueError("each list item must be one of ('GitHub', 'PW')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SyntheticPlaybackCapabilities from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SyntheticPlaybackCapabilities from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "browserType": obj.get("browserType"),
            "executionType": obj.get("executionType"),
            "extensions": obj.get("extensions"),
            "syntheticType": obj.get("syntheticType")
        })
        return _obj


